import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leoni.overtime',
  appName: 'Leoni - Overtime',
  webDir: '../ClientApp',
  bundledWebRuntime: false
};

export default config;
